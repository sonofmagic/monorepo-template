# Getting Started

The current setup guide is [repoctl Getting Started](./repoctl/getting-started.md).

```bash
pnpm add -D repoctl
pnpm exec repo init
pnpm exec repo doctor
```

Use [Adopt Existing Repositories](./repoctl/adopt-existing.md) when introducing repoctl to an established workspace.
