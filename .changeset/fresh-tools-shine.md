---
"@icebreakers/monorepo": patch
"repoctl": patch
---

Upgrade the bundled Icebreakers ESLint and Stylelint presets so repoctl/tooling uses the upstream peer dependency ownership fixes instead of carrying lint ecosystem dependencies in the monorepo runtime package.
