---
'@icebreakers/monorepo': patch
'repoctl': patch
---

Clarify npm package metadata so `repoctl` is presented as the default task-first CLI, while `@icebreakers/monorepo` is described as the underlying engine and compatibility package.
