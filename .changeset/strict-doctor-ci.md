---
"@icebreakers/monorepo": patch
"@icebreakers/monorepo-templates": patch
---

Add `repo doctor --strict` so CI can fail on warnings, and document the strict diagnostic flow in the VitePress starter guide.
