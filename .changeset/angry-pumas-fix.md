---
'@icebreakers/monorepo': patch
---

Allow `defineMonorepoVitestConfig()` to accept inline overrides so `vitest.config.ts` can customize shared defaults without manually spreading nested config objects.
