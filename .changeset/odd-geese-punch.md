---
'@icebreakers/monorepo': patch
---

Add short `define*Config()` tooling helpers and switch config entrypoints to the simplified names while keeping `defineMonorepo*Config()` as backward-compatible aliases.
