---
"create-repoctl": patch
"create-icebreaker": patch
---

新增 `create-repoctl` 包，提供 `npm create repoctl`、`pnpm create repoctl`、`yarn create repoctl` 的 repoctl 品牌脚手架入口，并补齐 `create-icebreaker` 的包导出以便复用现有创建流程。
